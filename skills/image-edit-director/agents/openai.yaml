interface:
  display_name: "Image Edit Director"
  short_description: "Create strict prompts for garment, dressed-model, face, scene, pose, and group behavior edits."
  default_prompt: "Use $image-edit-director to create a strict fashion image editing prompt with face, outfit, pose, hand ownership, and natural group behavior constraints."
policy:
  allow_implicit_invocation: true
